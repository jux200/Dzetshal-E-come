
document.getElementById('loginForm').addEventListener('submit', async e=>{e.preventDefault(); const email=document.getElementById('email').value.trim(); const password=document.getElementById('password').value; const btn=e.submitter; btn.disabled=true; btn.textContent='Signing in...'; const {data,error}=await sb.auth.signInWithPassword({email,password}); if(error){toast(error.message); btn.disabled=false; btn.textContent='Sign In'; return;} const {data:adm,error:admErr}=await sb.from('admin_users').select('*').eq('email',email).maybeSingle(); if(admErr||!adm){await sb.auth.signOut(); toast('This email is not authorized.'); btn.disabled=false; btn.textContent='Sign In'; return;} location.href='dashboard.html';});
